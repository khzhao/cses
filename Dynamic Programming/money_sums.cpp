#include <iostream>
#include <algorithm>
#include <climits>
#include <string>
#include <cstring>
#include <vector>
#include <array>

#define ll long long
#define ar array
#define f(i,n,m) for (int i=n; i<m; i++)

using namespace std;

int main() {
    int n;
    cin >> n;

    int coins[n];
    f(i,0,n) cin >> coins[i];
    sort(coins, coins+n);

    
     
}
