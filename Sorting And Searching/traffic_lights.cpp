#include <bits/stdc++.h>

#define ll long long
#define ar array
#define up(i,a,b) for(int i=a; i<b; i++)
#define down(i,a,b) for(int i=a; i>b; i--)
#define pb push_back

using namespace std;

int main() {
    int x, n;
    cin >> x >> n;

    vector<int> lights(n);
    up(i,0,n) cin >> lights[i];

    


}
